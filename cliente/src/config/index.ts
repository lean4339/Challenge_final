import en from './en'
import es from './es'

export const config = {
    spanish: {
        dictionary: es,
        language: "ES",
    },
    english: {
        dictionary: en,
        language: "EN"
    }
}